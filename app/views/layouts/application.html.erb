<!DOCTYPE html>
<html>
  <head>
    <title>FinalProject</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"></script>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg bg-light">
      <div class="container-fluid">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item"><a class="navbar-brand" href="#">My Fitness Tracker</a></li>
          <li class="nav-item"><a class="nav-link active" href="/">Home</a></li>
          <li class="nav-item"><a class="nav-link active" href="/exercises">Exercises</a></li>
          <% if session.fetch(:user_id) == nil %>
            <li class="nav-item"><a class="nav-link active" href="/user_sign_in">Sign In</a></li>
            <li class="nav-item"><a class="nav-link active" href="/user_sign_up">Sign Up</a></li>
          <% else %>
            <li class="nav-item"><a class="nav-link active" href="/user_sign_out">Sign Out</a></li>
            <li class="nav-item"><a class="nav-link active" href="/strength_comparisons">Strength Comparison</a></li>
            <li class="nav-item"><a class="nav-link active" href="/trackers">Trackers</a></li>
          <% end %>
        </ul>
      </div>
    </nav>
    <h5><span class="badge bg-success"><%= notice %></span></h5>
    <h5><span class="badge bg-danger"><%= alert %></span></h5>
    <%= yield %>
  </body>
</html>
